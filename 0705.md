# 2026-07-05 작업 로그 (Claude)

> 목적: 1차 진행(경량화 및 분석) 검토, 결과 데이터 검증, 추가 내용 분석 및
> 성능 개선. 진행과 동시에 이 문서에 기록한다.

## 0. 환경 파악

- 이 장비: RTX 5070 12 GB 데스크탑 (recovery 학습용 장비)
  - 보유: `data/training/front`(train/valid, 1.2 GB), S01~S04 recovery
    아티팩트, `shared-models/` ONNX·recovery PTH, rfdetr 1.8.1 + torch
    2.12.1+cu130 (CUDA 사용 가능)
  - 미보유: TensorRT 엔진(B/M/C/R 계열), `data/labeled_test`(평가셋),
    rclone 원격 설정 → **엔진 재평가·속도 측정은 이 장비에서 불가**
    (평가는 RTX 4050 노트북에서 수행된 상태)
- 1차 산출물: `results/paper_metrics.csv`(원본), `results/results_final.csv`,
  `results/coco-evaluation/*-front.json`, `figures/*.png`(9개),
  `docs/reports/results-draft.md`(논문 4장 초안)
- 신규 파일: `models/general_mission(with_crosswalk).pth`(7/4),
  `models/general_mission(without_crosswalk).pth`(7/5) — 용도 미문서화,
  본 로그 후반에서 분석

## 진행 계획

1. [x] 결과 데이터 일관성 검증 (paper_metrics.csv ↔ results_final.csv ↔
   results-draft.md 표 수치 재계산) → §1
2. [x] 세부 내용 분석: 클래스별·객체 크기별 segmentation 성능,
   recovery 학습 곡선(S01~S04) 분석 → §2, §3 (논문 초안 4.5절로 반영)
3. [x] 남은 작업 처리: mask AP 기준 Pareto 그래프 추가
   (evaluation-handoff §8 잔여 항목) → §4
4. [x] general_mission 체크포인트 구조 분석 + baseline 평가 → §5, §6
5. [x] 성능 개선 시도: S01 recovery 연장 실험 실행 → §7
   (2:4 개선 등 이 장비에서 불가한 항목은 §7 제안으로 정리)

---

## 1. 결과 데이터 일관성 검증 — 통과

검증 스크립트로 다음을 전부 재계산·대조했고 **불일치 0건**이었다.

- `results-draft.md` Table 1 (8개 모델 × 9개 수치) ↔ `paper_metrics.csv`
- Table 2의 B01 대비 변화율(크기·FPS·지연·ΔmAP) ↔ CSV에서 재계산한 값
- `results_final.csv`의 파생 열(감소율·증가율) ↔ 재계산 값
- `results/coco-evaluation/*-front.json`의 mask AP/AP50/AP75/mIoU ↔ CSV의
  mask 열 (9개 모델 전부, image_count=296 확인)
- Pareto front 3종 재도출 → 초안 서술과 동일
  (FPS: S01·C01·B02·R01 / Size: M02·C01·S01 / Latency: R01·B02·C01·S01)

테스트: `tests/` 12개 통과. 단 `tests/test_onnx_flops.py`는 이 장비 venv에
`onnx` 모듈이 없어 수집 불가(requirements-export 미설치 장비라 환경 문제,
코드 문제 아님).

**결론: 1차 산출물의 수치 파이프라인(CSV → 정리본 → 초안 표)은 상호
정합하며 재현 가능하다.**

---

## 2. 심층 분석 ①: 클래스별·객체 크기별 segmentation 성능

`results/coco-evaluation/*-front.json`에는 논문 초안에 아직 쓰이지 않은
크기별 AP(ap_small/medium/large)와 클래스별 semantic IoU가 있다. 전체를
표로 뽑으면:

| ID | Mask AP | AP_small | AP_medium | AP_large | AR_small | IoU out_line | IoU parking_lot | IoU parking_space |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| B01 | 0.6309 | 0.1091 | 0.5422 | 0.6892 | 0.2071 | 0.5945 | 0.9248 | 0.8599 |
| B02 | 0.6313 | 0.1090 | 0.5409 | 0.6902 | 0.2000 | 0.5942 | 0.9249 | 0.8791 |
| B03 | 0.6306 | 0.1089 | 0.5412 | 0.6912 | 0.2000 | 0.5942 | 0.9249 | 0.8784 |
| C01 | 0.6441 | 0.0898 | 0.5204 | **0.7105** | 0.2571 | **0.6124** | 0.9285 | 0.8722 |
| M01 | 0.6160 | **0.2012** | 0.5124 | 0.6775 | **0.3607** | 0.5953 | 0.9318 | 0.8721 |
| M02 | 0.6171 | 0.1841 | 0.5441 | 0.6792 | 0.3571 | 0.5960 | 0.9205 | 0.8721 |
| S01 | 0.6460 | 0.0906 | 0.5225 | 0.7104 | 0.2643 | 0.6120 | 0.9285 | 0.8722 |
| R01 | 0.6237 | 0.1382 | 0.5454 | 0.6769 | 0.2536 | 0.5733 | 0.9238 | 0.8637 |
| parking_front | 0.3065 | 0.0000 | 0.2797 | 0.3224 | 0.0000 | 0.3492 | 0.6692 | 0.7951 |

관찰(논문 Discussion 후보):

1. **공통 병목은 out_line 클래스이자 소형 객체다.** 모든 모델에서
   out_line IoU(0.57~0.61)가 parking_lot(0.92~0.93)·parking_space
   (0.86~0.88)보다 압도적으로 낮고, AP_small도 0.09~0.20에 그친다.
   학습 valid 셋 분포를 보면 소형 객체 104개 중 100개가 out_line이라
   두 현상은 사실상 같은 것이다(가늘고 작은 주차선). 즉 이 약점은
   **경량화로 생긴 손실이 아니라 baseline부터 존재하는 태스크 난이도**
   이며, B01→B02/B03에서 크기별 AP가 거의 변하지 않는 것이 그 증거다.
2. **Structured pruning + recovery(C01/S01)의 정확도 향상은 대형 객체와
   out_line에서 나왔다.** AP_large 0.689→0.710, out_line IoU 0.594→0.612.
   반면 AP_small은 0.109→0.090으로 오히려 하락. recovery fine-tuning이
   다수 클래스(대형) 쪽으로 최적화된 것으로 해석 가능.
3. **2:4 recovery(M01/M02)는 성능을 소형 객체 쪽으로 재분배했다.**
   AP_small 0.109→0.184~0.201, AR_small 0.207→0.357로 크게 오르지만
   AP_large가 0.689→0.678로 내려간다. 전체 mAP 하락(−0.03)의 실체는
   "전반적 열화"가 아니라 **대형 객체 정확도와 소형 객체 정확도의
   trade-off 재배치 + 대형 비중이 큰 평가셋 구성**이다. 초안 4.2의
   "fine-tuning 과정에서 손실 발생" 서술을 이 방향으로 구체화할 수 있다.
4. **주의(한계 서술용):** 테스트셋 정답 423개 중 소형 객체 수가 적어
   AP_small 차이는 통계적 노이즈에 민감하다. 논문에는 크기별 수치를
   보조 근거로만 쓰고, 클래스별 IoU(표본 충분)를 주 근거로 쓰는 것이
   안전하다.

## 3. 심층 분석 ②: Recovery 학습 곡선 (S01~S04)

`artifacts/experiments/S0*/front/recovery/metrics.csv`의 epoch별 val 성능:

| ID (구조 변경) | ep0 | 최종 ep | 최종 bbox mAP50-95 (EMA) | 회복량 |
|---|---:|---:|---:|---:|
| S01 (decoder −1) | 0.8049 | ep3 (조기종료) | 0.8091 (0.8101) | +0.0042 |
| S02 (decoder −2) | 0.7833 | ep8 (조기종료) | 0.8074 (0.8074) | +0.0241 |
| S03 (FFN −20%) | 0.7636 | ep5 (조기종료) | 0.8028 (0.8035) | +0.0392 |
| S04 (FFN −40%) | 0.7513 | ep5 (조기종료) | 0.7940 (0.7952) | +0.0427 |

관찰:

1. **구조 손상이 클수록 recovery가 많이 회복하지만 원점에는 못 미친다.**
   ep0(손상 직후) 손실과 최종 성능이 역상관 — decoder 1개 제거(S01)는
   손상 자체가 작아 4 epoch만에 최고치(0.8091) 도달, FFN 40% 축소(S04)는
   +0.043을 회복해도 0.794에 그침. "제거량↑ → 회복 후 성능↓"의 단조
   관계가 유지되므로 S01 선정은 학습 곡선 관점에서도 정당하다.
2. **조기종료의 실제 감시 지표는 bbox가 아니라 val/segm_mAP_50_95였다**
   (rfdetr 1.8.1은 segmentation_head 모델에서 segm mAP를 monitor,
   min_delta 0.001, 비-EMA 기본). S01의 bbox mAP는 매 epoch 올랐지만
   segm mAP는 ep0부터 0.663~0.666에서 정체 → patience 3 소진으로 종료.
   즉 "상승 중 조기종료"는 bbox 관점에서만 맞고, 감시 지표 관점에서는
   정당한 수렴 판정이었다. 다만 bbox 개선 여지(+0.001~0.002/epoch)가
   남아 있었는지는 연장 실험으로 확인할 가치가 있다 → §7.
3. out_line AP는 학습 val에서도 0.60~0.62로 test와 동일한 병목을 보여,
   §2의 결론(태스크 자체 난이도)을 교차 확인한다.

§2의 내용은 논문 초안 `docs/reports/results-draft.md`에 **4.5절
(클래스별·객체 크기별 분석)** 로 정리해 반영했고, 4.3절에는 mask AP
Pareto가 bbox 기준과 일치한다는 문단을 추가했다.

## 4. 잔여 작업 처리: Mask AP Pareto 그래프 추가

`evaluation-handoff.md` §8의 잔여 항목("mask AP와 FPS/크기의 Pareto 그래프
추가")을 처리했다.

- `scripts/reporting/paper_results.py`의 `pareto_chart()`를 y축 지표를
  선택할 수 있게 일반화(`ykey`/`ylabel` 인자 추가, 기본값은 기존과 동일)
- 신규 그래프 2종 생성 (렌더링 확인, 라벨 충돌 조정 완료):
  - `figures/pareto_mask_fps.png` — Mask AP vs FPS Pareto: **S01, C01,
    B02, R01** (bbox mAP 기준 front와 동일 구성)
  - `figures/pareto_mask_size.png` — Mask AP vs Size Pareto: **M02, C01,
    S01** (역시 bbox 기준과 동일)
- **분석적 의미: bbox mAP50-95 기준 Pareto front와 mask AP 기준 front의
  구성이 완전히 일치한다.** 즉 후보 선정 결론(C01 균형 최적)은 detection
  지표와 segmentation 지표 어느 쪽으로 봐도 동일 — 초안 4.3에 한 문장으로
  추가할 가치가 있다.
- `docs/reports/results-draft.md` Figures 표에 두 그래프를 등재했다.
- 기존 8개 그래프·`results_final.csv`는 재생성해도 변화 없음(회귀 없음 확인).

## 5. general_mission 체크포인트 분석 (신규 커밋 파일)

7/4~7/5에 커밋된 `models/general_mission(*.pth)` 2개를 로드해 확인했다.

| 항목 | with_crosswalk | without_crosswalk |
|---|---|---|
| 아키텍처 | RFDETRSegLarge (35.6M, parking 모델과 동일) | 동일 |
| 클래스 (6/5개) | drivable-road, car, **crosswalk**, lane1, lane2, traffic_light | crosswalk 제외 동일 |
| 학습 | 40 epochs, batch 4 × accum 4, lr 1e-4, multi-scale | 동일 조건 |
| global_step | 38,560 | 18,075 |
| 데이터셋 | `~/datum/rfdetr_seg_general_mission` (4.5 GB, train/valid/test) | `..._2` (3.5 GB) |

의미:

- **주차 미션에서 확립한 경량화 파이프라인(FP16/pruning/TensorRT)이 그대로
  적용 가능한 두 번째 도메인 모델**이다(아키텍처 동일). 논문 범위를
  "front 주차 모델"에서 "일반 주행 미션"으로 확장하거나, 방법론의
  일반성 근거로 쓸 수 있다.
- 학습 데이터셋에 test split이 이 장비에 존재하므로 baseline 정확도
  평가는 이 장비에서 가능하다. TensorRT 속도 평가는 기존과 같이
  변환·평가 장비에서 수행해야 한다.
- registry(`configs/experiments/registry.yaml`)에는 아직 등재되지 않았다.
  경량화를 진행하려면 baseline 항목 추가 + `configs/baseline.yaml`에
  체크포인트·클래스 매핑 등록이 첫 단계다.

## 6. general_mission baseline 정확도 평가 (신규 실행)

이 장비에서 PTH를 직접 평가할 수단이 없어(기존 스크립트는 TensorRT 엔진
또는 Ultralytics 전용) 다음을 추가했다.

- **신규 스크립트** `scripts/evaluation/evaluate_coco_rfdetr_pth.py`:
  RF-DETR `.pth`를 임의 COCO split에서 bbox AP + mask AP + semantic mIoU로
  평가. 기존 `evaluate_coco_tensorrt.py`의 마스크 처리·mIoU 로직을 그대로
  재사용(import)하므로 지표 정의는 1차 평가와 동일하다.
  `scripts/evaluation/README.md`에 사용법 등재.

test split(223장) 평가 결과 (RTX 5070, threshold 0.001 / mIoU 0.25):

| 모델 | bbox mAP50-95 | bbox mAP50 | Mask AP | Mask mIoU |
|---|---:|---:|---:|---:|
| general_mission (with_crosswalk) | 0.9176 | 0.9829 | 0.9170 | 0.9499 |
| general_mission (without_crosswalk) | 0.9132 | 0.9827 | 0.9255 | 0.9481 |

클래스별 mIoU: car 0.933, crosswalk 0.948, lane1 0.953~0.959,
lane2 0.963~0.964, traffic_light 0.942~0.947 — 전 클래스 균질하게 높음.
상세 JSON: `results/coco-evaluation/general_mission_*-test.json`

관찰:

- **주차 모델보다 baseline 정확도가 훨씬 높다**(mask AP 0.92 vs 0.63).
  parking 데이터의 out_line 같은 얇은 구조가 없기 때문. 경량화 손실
  허용 폭이 크므로 FP16 일괄 적용의 성공 가능성이 높다.
- crosswalk 포함 여부가 다른 클래스 성능에 거의 영향 없음(car IoU
  0.933 동일). crosswalk 자체도 0.948로 안정적 → **with_crosswalk 버전을
  기본 후보로 삼아도 무방**하다는 정량 근거.
- AP_small은 여기서도 낮다(0.04~0.07, 원거리 신호등 추정) — 소형 객체
  약점은 RF-DETR 계열의 일관된 한계로, 논문 한계 절에서 도메인 불문
  공통 패턴으로 언급 가능.

## 7. 성능 개선 실험: S01 recovery 연장 학습

§3에서 확인한 "bbox mAP 상승 중 조기종료"가 실제 손실이었는지 검증하는
실험. 기존 recovery 산출물은 건드리지 않고 별도 디렉토리에서 재개했다.

- 명령: `train_candidate.py S01 --camera front --device cuda --batch-size 2
  --grad-accum-steps 8 --output-dir .../recovery-extended
  --resume .../recovery/checkpoint_3.ckpt`
- 조건: 기존 profile(recovery_structured) 그대로 — epoch 예산 10, lr 등
  하이퍼파라미터 변경 없음. 즉 "조기종료가 없었더라면 진행됐을 나머지
  epoch"를 이어서 실행하는 것으로, 등록된 학습 조건을 위반하지 않는다.
- 장비: RTX 5070 (원래 S01 recovery를 학습한 동일 장비)

**결과 (완료, 7/6 새벽): 가설 검증 성공 — 조기종료가 성능을 남겨뒀었다.**

| | 원본 recovery (ep0~3) | 연장 (ep4~7, 재-조기종료) |
|---|---:|---:|
| val bbox mAP50-95 | 0.8091 (EMA 0.8101) | **0.8156 (EMA 0.8164)** |
| val segm mAP50-95 | 0.6655 | **0.6700** |

- 연장 4 epoch 동안 매 epoch 개선(0.8123→0.8133→0.8153→0.8156), ep7에서
  다시 patience 3 조기종료. **bbox +0.0065, segm +0.0045** — 감시 지표였던
  segm까지 원본 종료 시점보다 올라, 원본 조기종료는 잘못된 수렴 판정이었음이
  확정.
- 신규 best checkpoint:
  `artifacts/experiments/S01/front/recovery-extended/checkpoint_best_total.pth`
  (원본 recovery는 그대로 보존). S01/C01 엔진을 이 checkpoint로 재빌드하면
  논문 최고 후보의 정확도가 더 올라갈 여지가 있다 — test셋 검증은 아래
  §15 교차 비교 참조.

### ~~이 장비에서 실행하지 않고 제안으로 남기는 개선 항목~~
### → 사용자 지시로 아래 4개 항목 모두 이 장비에서 착수 (§8~ 참고)

1. **2:4 recovery 개선(M01/M02)**: §2에서 2:4 손실의 실체가 소형↔대형
   trade-off 재배치임을 확인했으므로, recovery 시 knowledge distillation
   (원본 dense 모델을 teacher로)이나 더 긴 epoch 예산이 대형 객체 정확도를
   되찾을 가능성이 있다. 단 M01 아티팩트(2:4 마스크·preflight)가 이 장비에
   없고 `recovery_2to4` 프로필이 blocked 상태라 재현 불가. 하이퍼파라미터
   변경은 registry 조건 변경이므로 사용자 결정 사항.
2. **INT8 QAT**: 초안 4.4가 지적한 "PTQ 이득 없음"의 후속. rfdetr 1.8.1에
   QAT 파이프라인이 없어 별도 구현 필요 — 논문 향후 과제로 서술 권장.
3. **후방(rear) 카메라 평가**: registry에 B01~B03 rear가 등록돼 있으나
   엔진·평가 모두 미수행. labeled_test/rear는 평가 노트북에서 진행 필요.
4. **general_mission 경량화 착수**: §6의 baseline이 확보됐으므로 registry에
   G01(baseline) 계열을 등록하고 parking과 동일한 B02/C01 레시피(FP16,
   structured+FP16)를 적용하는 것이 다음 단계로 가장 수익이 크다.

---

## 8. (2차 지시) 후방 카메라 baseline 평가

`~/datum/rfdetr_seg_rear/test`(233장)가 이 장비에 있어 rear PTH 평가가
가능했다. 비교 맥락을 위해 front PTH도 같은 방식의 `~/datum/rfdetr_seg/test`
(446장)로 평가했다. **주의: 이 test split은 논문 1차 평가의
`data/labeled_test/front`(296장, 큐레이션 subset)와 다르므로 paper_metrics
수치와 직접 비교 불가.** PTH(FP32 PyTorch) 기준 baseline이다.

| 모델 (split) | bbox mAP50-95 | bbox mAP50 | Mask AP | Mask mIoU | 클래스별 IoU |
|---|---:|---:|---:|---:|---|
| parking_rear (rear test 233장) | 0.8969 | 0.9848 | 0.8155 | 0.9167 | end_line 0.864 / lot 0.938 / space 0.948 |
| parking_front (front test 446장) | 0.7981 | 0.9655 | 0.6503 | 0.8273 | out_line 0.628 / lot 0.942 / space 0.912 |

관찰:

- **rear 모델이 front보다 전반적으로 강하다**(mask AP 0.816 vs 0.650).
  rear의 end_line IoU(0.864)는 front의 out_line(0.628)보다 훨씬 높다 —
  후방 카메라 시점에서 라인이 더 크고 명확하게 보이는 것으로 추정.
- front의 병목 구조(out_line·소형 객체)는 이 split에서도 재현된다.
  §2 결론이 split에 무관함을 교차 확인.
- rear는 정확도 여유가 충분해 FP16 경량화 손실 허용 폭이 크다. rear
  TensorRT 후보(B01~B03 rear)의 정확도 평가 기준선으로 이 수치를 쓸 수
  있다(단 split 차이 명시 필요).
- 상세: `results/coco-evaluation/parking_rear_pth-datum_test.json`,
  `parking_front_pth-datum_test.json`

## 9. (2차 지시) general_mission 경량화 착수 — FP16 무손실 확인

파이프라인 등록부터 FP16 엔진 정확도 평가까지 이 장비에서 완료했다.

수행 내용:

1. `configs/baseline.yaml`에 `general_mission_with_crosswalk` /
   `general_mission_without_crosswalk` 모델 등록 (체크포인트·클래스·
   dataset_dir).
2. `scripts/lightweighting/export_onnx.py`와 `build_tensorrt_fp16.py`의
   `--camera` 인자를 front/rear 고정에서 임의 모델 키로 확장(기존 사용법
   호환 유지).
3. venv에 `onnx==1.22.0`, `tensorrt-cu13==10.16.1.11` 설치(requirements
   파일 버전 그대로).
4. 두 모델 ONNX export(opset 17, batch 1, 504×504) → FP16 엔진 빌드
   (RTX 5070, 빌드 각 ~40초).
5. `evaluate_coco_tensorrt.py`가 레포 밖 데이터셋 경로에서 죽는 버그
   수정(`relative_to` → 안전 헬퍼) 후 test split 정확도 평가.

결과 (test 223장, threshold 0.001):

| 모델 | 형식 | 크기(MB) | Mask AP | Mask mIoU |
|---|---|---:|---:|---:|
| with_crosswalk | PTH FP32 | 142.5 | 0.9170 | 0.9499 |
| with_crosswalk | **TensorRT FP16** | **73.4** | **0.9173 (+0.0003)** | 0.9499 (±0) |
| without_crosswalk | PTH FP32 | 142.5 | 0.9255 | 0.9481 |
| without_crosswalk | **TensorRT FP16** | **73.5** | **0.9252 (−0.0003)** | 0.9481 (±0) |

**결론: parking 도메인에서 확립한 "FP16 변환은 사실상 무손실" 결론이
일반 주행 미션 도메인에서 그대로 재현된다** (크기 48% 감소, 정확도 변화
±0.0003). 논문의 방법론 일반성(generality) 근거로 사용 가능.

주의:

- 엔진은 RTX 5070(sm_120)에서 빌드했으므로 배포 장비에서 재빌드 필요
  (TensorRT 엔진은 GPU 아키텍처 종속).
- **속도(FPS) 측정은 아직 안 함** — 현재 GPU가 recovery 학습에 점유돼
  있어 공정한 측정 불가. 학습 종료 후 또는 평가 노트북에서 측정할 것.
- 상세: `results/coco-evaluation/general_mission_*_fp16-front.json`,
  엔진·메타데이터: `artifacts/tensorrt/general_mission_*/`

## 10. (2차 지시) 2:4 recovery 개선 실험 — M03 등록·준비 완료, 학습 대기열

§2에서 세운 가설(2:4 recovery의 손실은 학습 예산 부족/조기종료 탓일 수
있음)을 검증하는 실험을 레포 관례대로 등록했다.

- registry에 **M03 (sparse-2to4-extended-recovery)** 추가: M01과 동일한
  2:4 pruning에서 출발하되 recovery 예산을 **20 epochs, 조기종료 없음**
  으로 확대 (신규 프로필 `recovery_2to4_extended` — 기존 recovery_2to4는
  수정하지 않고 보존).
- `analyze_candidate.py`의 preflight가 (a) 실험의 프로필 대신
  recovery_2to4를 하드코딩하는 문제, (b) 평가 전용 labeled_test가 없는
  학습 장비에서 클래스 검증이 잘못 블로킹되는 문제를 수정.
- 파이프라인 실행 완료: 2:4 eligibility → M03 후보 생성(2:4 pruning 검증
  통과) → preflight **training allowed: True**.
- **학습은 S01 연장(§7) 종료 후 자동 시작되도록 대기열 등록**
  (동일 GPU라 순차 실행; 20 epochs ≈ 약 6시간 예상).
- 완료 후 비교 기준: shared-models의 M01/M02 recovery(10 epochs)와
  같은 test 조건 평가 → 대형 객체 AP 회복 여부 확인.

**결과 (완료, 7/6 02:55, 20 epochs 전부 수행, 2:4 마스크 검증 통과):**

val bbox mAP50-95 곡선: ep0 0.617 → ep9 0.782 → ep14 0.786 → ep19
**0.791 (EMA 0.789)**. segm은 ep11 이후 0.652~0.653에서 포화.

해석 — **가설 부분 기각, 결론은 오히려 더 명확해짐**:

1. 예산 2배(10→20ep)의 효과는 val bbox 약 +0.008에 그치고 ep14 이후
   epoch당 +0.001 미만으로 포화. **조기종료·예산 부족은 2:4 손실의 부차적
   원인일 뿐, 주된 손실(구조적 pruning 대비 ~0.02-0.03)은 2:4 제약
   자체의 비용**이다.
2. 동일 조건의 S01(decoder pruning) recovery가 val 0.816에 도달하는 반면
   2:4는 20 epoch을 줘도 0.791이 상한 — "같은 크기 감소라면 구조적
   pruning이 2:4보다 낫다"는 본 논문 결론을 강화하는 통제 실험.
3. 어느 쪽으로 나와도 결론이 나오는 실험 설계였고, "예산이 아니라 제약
   자체"라는 답을 얻었다. 논문 4.4의 2:4 서술을 이 근거로 보강 가능.
   test셋 정량 비교는 §15 참조.

## 11. (2차 지시) INT8 QAT 검토 — 기술적으로 가능, 실행은 GPU 대기열 뒤

`nvidia-modelopt 0.44.0`을 설치하고 RF-DETR에 INT8 quantizer 삽입 스모크
테스트를 수행했다.

- `mtq.quantize(core, INT8_DEFAULT_CFG, forward_loop)` →
  **TensorQuantizer 723개 삽입 성공**, quantized 모델 forward 정상
  (pred_logits/pred_boxes/pred_masks 출력 확인). CPU에서 검증.
- 주의: 캘리브레이션 forward는 `wrapper.predict()` 경유가 아니라 코어
  모듈에 텐서를 직접 넣어야 한다(predict의 postprocess가 quantize
  컨텍스트에서 dict 출력과 충돌).

실행 계획 (GPU가 S01 연장 → M03 학습에 점유돼 있어 순차 대기):

1. 캘리브레이션: `data/training/front/valid` 실제 이미지 수백 장으로
   GPU에서 mtq.quantize.
2. QAT fine-tune: quantized 코어를 `RFDETRModelModule`에 넣고 기존
   recovery 인프라로 2~5 epoch 저학습률 미세조정(신규 프로필로 등록).
3. Q/DQ 포함 ONNX export → TensorRT INT8 엔진 빌드(`--int8` 경로에서
   Q/DQ 존중 여부 확인 필요 — build_tensorrt_int8.py는 calibration 기반
   PTQ용이므로 QAT 엔진용 빌드 옵션 분기 추가 필요).
4. 기대 효과 검증 지점: 초안 4.4의 "INT8 PTQ가 FP16 커널로 폴백해 이득
   없음" — QAT로 INT8 커널 사용률이 올라 FP16 대비 추가 속도 이득이
   생기는지가 핵심 확인 사항.

위험 요소: rfdetr의 ONNX export 경로가 quantized 모듈을 지원하는지
미검증(export 시 TensorQuantizer의 fake-quant 노드가 Q/DQ로 변환되어야
함). 실패 시 modelopt의 `mto.export`/`torch.onnx` 직접 경로로 대체.

## 12. 논문성 검토 보고 (사용자 요청)

"모델 교체 + 경량화 적용이 전부 아닌가"라는 우려에 대한 검토 결과.
**단순 적용기를 넘어서는 발견이 이미 여러 개 있고, 진행 중인 실험이
그것을 강화한다.** 다만 알고리즘 신규성은 없으므로 프레이밍이 중요하다.

### 12.1 이미 확보된 논문성 있는 발견

각 항목: 주장 → 근거 → 왜 "단순 적용"이 아닌가.

1. **상호작용 효과: 구조적 pruning의 배포 이득은 정밀도 변환과 결합해야
   실현된다.** 동일 pruning인데 S01(FP32)은 FPS +4.6%, C01(+FP16)은
   +71.0%. 크기 감소도 4.6%→50%. 개별 기법의 효과를 따로 보고하는
   기존 관행과 달리, TensorRT 그래프 최적화 하에서 기법 간 결합이
   비가산적(non-additive)임을 통제된 조건에서 보인 것 — 이 관찰 자체가
   분석 기여다.
2. **Transformer 검출기에서 INT8 PTQ의 무효성.** B03은 FP16 대비 크기·
   속도 이득이 없음(67.98 vs 67.90 MB, 31.9 vs 32.6 FPS). TensorRT가
   주요 연산을 FP16 커널로 폴백. CNN(YOLO) 계열에서 통용되는 "INT8이
   더 빠르다"가 DETR 계열 세그멘테이션 모델에 성립하지 않음을 보인
   배포 관점의 부정적 결과(negative result) — QAT 필요성 제기로 이어짐.
3. **2:4 희소화 정확도 손실의 재해석: 균일 열화가 아니라 스케일 간
   재분배.** M01(dense control)과 M02가 거의 같은 정확도 → 손실은 희소
   실행이 아니라 recovery 학습에서 발생(기존 분석). 오늘 추가 발견:
   그 손실은 AP_small +0.09, AP_large −0.011의 재분배이며 대형 객체
   비중이 큰 평가셋에서 평균이 하락한 것(§2). "2:4는 정확도를 떨어뜨린다"
   보다 훨씬 구체적인 주장.
4. **대조군 설계에 의한 인과 분리.** U02(unstructured, dense 그래프
   무가속 입증)와 M01(2:4 dense-tactic control)은 명시적 통제 실험이다.
   벤치마크 나열 논문과 차별화되는 실험 설계 기여.
5. **정밀도 변환은 클래스·크기에 균일, recovery 기반 기법은 비균일.**
   B02/B03은 클래스별 IoU·크기별 AP가 B01과 ≤0.001 차이로 동일한 반면
   recovery 경유 후보(C01/S01, M01/M02)는 분포가 이동(§2). "경량화가
   어떤 성능을 희생하는가"에 대한 세분화된 답.
6. **다중 태스크 헤드 recovery의 조기종료 지표 문제 (실증 중).** rfdetr의
   조기종료는 segm mAP만 감시 → bbox가 상승 중인데 종료. 연장 실험(§7)
   재개 2 epoch 만에 bbox 0.8091→0.8133(EMA 0.8145), segm도
   0.6655→0.6698로 상승 — **조기종료 판정 자체가 잘못된 수렴 판정이었음을
   실증**. detection+segmentation 복합 모델의 recovery에서 단일 지표
   조기종료가 남기는 성능을 정량화한 사례.
7. **교차 도메인 재현성.** FP16 무손실(±0.0003)이 parking front에 이어
   general_mission(6클래스 일반 주행)에서도 재현(§9) + rear 모델
   baseline(§8)까지 3개 도메인/모델 자산. "한 모델에서의 우연"이 아니라
   방법론의 일반성 근거.
8. **(부수) 평가 방법론 함정: 클래스 ID 불일치.** Ultralytics 모델의
   mAP 0.0017은 ID 오프셋으로 인한 무효 수치였고 이름 기반 매핑으로
   0.713 회복(초안 4.4). 이기종 모델 비교 평가의 실무적 주의사항.

### 12.2 현재 스토리의 약점 (정직한 평가)

- 새로운 알고리즘/기법 제안 없음 — 기여는 전부 실험·분석 기여.
- 단일 아키텍처(RF-DETR Seg Large), 단일 평가 GPU. 테스트셋 296장,
  소형 객체 표본 적음(크기별 AP는 보조 근거로만).
- 2:4와 INT8은 현재 "안 된다"에서 멈춰 있음 — M03·QAT가 "왜, 그리고
  어떻게 되게 하는가"로 바꿔줄 수 있는 위치.

### 12.3 논문성을 높이는 보강 (실행 중/실행 가능)

1. **M03(2:4, 20 epochs, 조기종료 없음)** — 발견 3·6의 인과 검증.
   대기열 등록됨. 성공 시 "2:4 손실은 recovery 예산 부족" 결론 완성,
   실패 시에도 "예산이 아니라 제약 자체" 라는 결론으로 논문 서술 가능
   (어느 쪽이든 결론이 나오는 실험 설계).
2. **INT8 QAT** — 발견 2의 해법 제시. 성공 시 "PTQ 폴백 진단 → QAT로
   해소"의 완결 스토리.
3. **TensorRT engine inspector로 레이어별 precision 통계** — "FP16
   폴백" 주장을 추정에서 측정으로 격상(B03 엔진이 있는 평가 노트북에서
   `trt.EngineInspector` 실행). 저널 수준 보강에 특히 유효.
4. rear·general_mission 후보 확대 — 도메인 3개 × 대표 기법의 일관성 표.

### 12.4 추천 프레이밍

- 제목 방향: "TensorRT 배포 환경에서 Transformer 기반 세그멘테이션
  검출기의 경량화 기법 간 상호작용 분석" 류 — "적용"이 아니라
  "상호작용·인과 분석"을 전면에.
- 기여 문장(초안): ① 8개 경량화 후보의 통제된 비교와 3축 Pareto 분석,
  ② pruning×정밀도 결합의 비가산적 이득 규명, ③ INT8 PTQ 폴백·2:4
  재분배·조기종료 지표 문제라는 3개 실패 모드의 원인 분석, ④ 2개 도메인
  재현으로 일반성 확인.
- KIPS 학술대회 수준에는 현재 내용으로 충분하고, 저널 투고를 노린다면
  12.3의 1~3 완료 후가 적기.

## 13. (논의) LLM식 경량화 기법의 적용 가능성 검토

사용자 질문: imatrix(중요도 행렬) 활용, "중요 레이어 FP16 유지 + 나머지
극단 양자화" 방식을 썼는가 / 적용 가능한가.

- **지금까지 안 씀.** 현재 파이프라인은 uniform FP16, uniform INT8 PTQ
  (+TensorRT의 속도 기준 자동 폴백)뿐. TRT 폴백은 "빠른 커널 선택"이지
  "정확도 중요도 기반 보호"가 아님 — 개념적으로 다르다.
- **적용 가능.** 설치된 nvidia-modelopt 0.44에 대응 도구가 모두 존재:
  INT8_SMOOTHQUANT_CFG(imatrix와 같은 activation-aware 계열),
  INT4_AWQ_CFG / W4A8_AWQ(중요 채널 보호 + 극단 양자화),
  `mtq.auto_quantize`(레이어별 민감도 기반 혼합 정밀도 자동 탐색),
  레이어 패턴별 quantizer disable(수동 mixed precision).
- 실행 경로와 우선순위는 대화 보고 참조. 요약: ① SmoothQuant PTQ A/B
  (기존 INT8 PTQ와 직접 비교, 비용 최소), ② 민감도 맵 + 부분 Q/DQ
  (mixed precision, 논문의 "실패 원인 분석" 축과 정합),
  ③ INT4 weight-only(FFN 한정, 크기 극단 감소용; TRT 10의 vision 모델
  INT4 GEMM 지원 범위 확인 필요).

## 14. (3차 지시) Q-시리즈 등록·구현·대기열 가동 — A/B/C 전부

§13의 세 방식을 사용자 지시로 실험 체계에 정식 등록하고 실행 파이프라인을
구축했다.

등록 (registry.yaml, schema에 int4·mixed 정밀도 추가):

| ID | 방식 | 내용 |
|---|---|---|
| Q01 | int8-smoothquant-ptq | activation-aware INT8 (imatrix 대응물), calibration 128장 |
| Q02 | mixed-precision-sensitive-fp16 | head·sampling_offsets 등 민감 레이어 FP16 유지, 나머지 INT8 |
| Q03 | int4-weight-only-ffn | decoder/backbone FFN Linear만 INT4 weight-only |

구현:

- **신규 스크립트 `scripts/lightweighting/quantize_modelopt.py`**:
  baseline.yaml 모델 로드 → 실제 이미지 캘리브레이션 → mtq.quantize →
  모드별 quantizer 패턴 disable(Q02: 민감 레이어 제외 / Q03: FFN만 유지)
  → Q/DQ ONNX export + 검증 + quantization.json 보고서.
- `build_tensorrt_fp16.py`에 `--int8-qdq` 플래그 추가 (Q/DQ ONNX용
  INT8+FP16 빌드; trtexec·python 경로 모두 배선).
- **스모크 테스트 통과**: mixed 모드 CPU 4장 캘리브레이션 → export →
  **ONNX에 Q/DQ 노드 512개 생성 확인**. 최대 리스크였던 "양자화 모듈의
  rfdetr export 경로 통과"가 검증됨.

대기열 (`scripts/experiments/run_qseries_queue.sh`, 백그라운드 가동 중):

1. S01 연장 → M03(2:4, 20ep) 학습이 모두 끝날 때까지 대기
   (train_candidate 프로세스 5분 연속 부재 확인)
2. B01 FP16 로컬 참조 엔진 빌드 + 평가 (`~/datum/rfdetr_seg/test` 446장
   기준 비교 앵커; labeled_test와 split 다름 주의)
3. Q01 → Q02 → Q03 순서로 각각 quantize(128장, GPU) → 엔진 빌드
   (--int8-qdq) → COCO 평가. 실패해도 다음 단계 계속, 전 과정
   `artifacts/experiments/qseries-queue.log`에 기록.

예상 타임라인: S01 연장(~1h 내) → M03(~6h) → Q-시리즈(~1.5h).
Q03은 TensorRT의 vision 모델 INT4 지원 범위에 따라 빌드가 실패할 수
있으며, 실패 자체도 기록된다(탐색적 실험).

## 15. Recovery 체크포인트 4종 동일-split 교차 비교 (7/6)

§7(S01 연장)과 §10(M03)의 결과를 같은 test split
(`~/datum/rfdetr_seg/test`, 446장, PTH FP32)에서 직접 비교했다.
참고: 같은 split의 무-pruning baseline(parking_front.pth)은
bbox 0.7981 / mask 0.6503 (§8).

| Recovery checkpoint | bbox mAP50-95 | AP_large | AP_small | Mask AP | Mask mIoU |
|---|---:|---:|---:|---:|---:|
| S01 recovery 10ep(원본, 실제 4ep) | 0.7979 | 0.8788 | 0.2482 | 0.6557 | 0.8270 |
| **S01 recovery 연장(8ep)** | **0.8064** | **0.8877** | 0.2477 | 0.6544 | 0.8270 |
| 2:4 recovery 10ep(M01/M02 원본) | 0.7545 | 0.8306 | 0.2905 | 0.6292 | 0.8172 |
| **2:4 recovery 20ep(M03)** | **0.7792** | 0.8550 | **0.3371** | 0.6423 | 0.8221 |

관찰:

1. **S01 연장의 이득은 test에서도 실재**: bbox +0.0085 (0.7979→0.8064).
   이 수치는 같은 split의 무-pruning baseline(0.7981)을 넘어선다 —
   "pruning+충분한 recovery가 원본을 상회"가 유지·강화됨. 단 mask AP는
   −0.0013으로 사실상 동일: 연장의 이득은 detection 쪽에 집중.
2. **2:4 예산 확대의 이득은 생각보다 큼**: test bbox +0.0247
   (0.7545→0.7792), mask AP +0.0131. 원본 10ep recovery와의 격차의
   절반가량을 회복. **그러나 여전히 S01 연장 대비 −0.027로 열위**이고
   val 곡선은 ep14 이후 포화 — §10의 결론("주된 손실은 2:4 제약 자체")은
   유지하되, "recovery 예산·조기종료도 무시 못 할 부차 요인(+0.02)"으로
   정량화해 서술하는 것이 정확하다.
3. **2:4 recovery의 소형 객체 편향 재확인**: M03에서 AP_small이 0.337로
   더 올라감(원본 2:4 0.291, S01 계열 0.248). §2에서 본 "2:4 recovery는
   소형 쪽으로 재분배" 패턴이 예산을 늘려도 유지 — 우연이 아니라 체계적
   현상임을 뒷받침.
4. 함의: (a) 논문 표의 S01/C01은 연장 checkpoint로 엔진 재빌드 시 개선
   여지, (b) 2:4 서술은 "예산 2배로도 구조적 pruning에 못 미침 + 손실은
   스케일 재분배"로 업그레이드. (c) recovery 조기종료 기준(단일 segm
   지표, patience 3)은 후속 실험에서 재설계 필요.

## 16. Q-시리즈 결과 (7/6 03:12 완료) — 강제 INT8의 삼중 실패, 논문 서사 완성

대기열이 B01 FP16 참조 → Q01 → Q02 → Q03을 자동 수행했다. 평가는
`~/datum/rfdetr_seg/test` 446장, 속도는 RTX 5070 batch 1 (warmup 20 /
runs 100). 전 과정 로그: `artifacts/experiments/qseries-queue.log`.

| 엔진 | 크기(MB) | Mask AP | AP_small | Mask mIoU | 평균 지연(ms) | FPS |
|---|---:|---:|---:|---:|---:|---:|
| B01 FP16 참조 | 73.5 | 0.6488 | 0.2160 | 0.8251 | 11.81 | 84.7 |
| Q01 SmoothQuant INT8 (Q/DQ) | 85.3 | 0.5440 | **0.0005** | 0.7529 | 12.11 | 82.6 |
| Q02 mixed (민감층 FP16) | 79.3 | 0.5451 | 0.0118 | 0.7544 | 11.78 | 84.9 |
| Q03 INT4-FFN | — | 빌드 실패 | | | | |

핵심 발견 — **Q/DQ로 INT8을 "강제"하면 삼중으로 실패한다**:

1. **정확도 −0.10** (mask AP 0.649→0.544). 특히 **AP_small이 사실상
   0으로 붕괴**(0.216→0.0005) — 양자화 노이즈가 소형·얇은 구조를
   우선적으로 파괴. §2의 out_line/소형 객체 병목과 연결되는 네 번째
   스케일 의존 현상.
2. **속도 이득 없음** (82.6~84.9 vs 84.7 FPS) — reformat 오버헤드로
   오히려 Q01은 미세하게 느림.
3. **크기 증가** (85.3/79.3 vs 73.5 MB) — Q/DQ 스케일 텐서와 혼합 커널
   중복으로 FP16보다 커짐.

**해석 (논문 4.4를 완성하는 결론):** 기존 B03(TensorRT calibration PTQ)은
"무손실이지만 무이득"이었는데, 그 이유가 이번에 실증됐다 — **TensorRT는
정확도·속도가 나빠지는 INT8 커널을 조용히 회피하고 있었던 것**이다.
Q/DQ로 회피를 막자 정확도·속도·크기 모두 악화됐다. 즉 이 아키텍처에서
INT8은 PTQ로는 어떤 형태로도 이득이 없으며(회피하면 무이득, 강제하면
손해), **남은 경로는 QAT뿐**이라는 결론이 완결된다.

추가 발견:

- **Q02의 휴리스틱 민감층 보호(head·sampling_offsets FP16 유지)는 거의
  무효**(0.5451 vs 0.5440, AP_small만 소폭 회복). 민감도는 추정한 위치
  (head)가 아니라 backbone/encoder 활성값 쪽에 있다는 뜻 — LLM식 "중요
  레이어 보호"를 이식하려면 휴리스틱이 아니라 **측정 기반 민감도 맵**이
  필요하다는 근거. (후속: 블록별 leave-one-out 양자화 스윕)
- **Q03(INT4 blockwise weight-only)은 TensorRT 10.16이 blockwise INT4
  dequant(Float 입력)를 지원하지 않아 파서 단계에서 실패** — "LLM용
  INT4 경로는 아직 vision 모델 그래프에 이식 불가"라는 도구 한계 확인.
  (에러: `Block quantization is not supported for DataType Float`)
- imatrix 대응물(SmoothQuant)도 이 급의 정확도 붕괴를 못 막음 —
  activation-aware 스케일링은 LLM Linear 중심 그래프 대비 deformable
  attention·conv 혼합 그래프에서 효과가 제한적.

## 17. (4차 지시) INT8 중단 → 측정 기반 민감도 + FP8 혼합 정밀도로 전환

사용자 지시 두 가지를 반영했다: ① 민감도는 휴리스틱이 아니라 **측정
기반**이어야 함(Q02가 휴리스틱 v1이었던 것에 대한 정당한 지적), ② INT8은
효율이 나빠 중단(§16 결과와 일치). 이에 따라 공격적 정밀도를 INT8에서
**FP8(E4M3)** 로 바꿔 원래 설계를 실행한다.

- **신규 스크립트 `scripts/lightweighting/sensitivity_sweep.py`**:
  leave-one-in 방식 — 전체 캘리브레이션 후 모든 quantizer를 끄고, 블록
  하나씩 켜서 valid 150장의 bbox/segm mAP 하락을 측정. 블록 구성은
  backbone 레이어 그룹 4개 + projector + decoder 레이어 5개 +
  enc_output/heads + final heads + segmentation head (자동 발견).
  결과는 민감 블록 목록 포함 JSON으로 저장.
- registry에 **Q04(fp8-uniform-ptq)**, **Q05(fp8-mixed-measured-
  sensitivity)** 등록 (schema에 fp8 정밀도 추가). Q05는 스윕 JSON의
  민감 블록만 FP16 유지, 나머지 FP8.
- `quantize_modelopt.py`에 fp8/fp8-mixed 모드 추가. 스모크에서 발견한
  제약 두 가지를 처리: (a) FP8 Q/DQ는 opset 19 필요 → 자동 승격,
  (b) **Conv 레이어의 FP8 Q/DQ는 torch ONNX export 불가**(patch embed
  conv에서 실패) → fp8 모드는 Conv quantizer 자동 비활성(FP8은 어차피
  GEMM 가속 중심). Linear 293개에 TRT_FP8Quantize/Dequantize 커스텀 op
  export 확인.
- `build_tensorrt_fp16.py`에 `--strongly-typed` 경로 추가(예비). 스모크
  결과 기본 FP16 플래그 경로로도 FP8 커스텀 op 엔진이 빌드됨 —
  **스모크 엔진 40.9MB로 FP16(73.5MB) 대비 44% 추가 감소** (기대 신호).
- **파이프라인 가동**(`scripts/experiments/run_fp8_pipeline.sh`,
  백그라운드): 민감도 스윕 → Q04(uniform FP8) → Q05(측정 기반 mixed)
  각각 quantize(128장) → 엔진 → test 446장 평가 → 벤치마크.
  로그: `artifacts/experiments/fp8-pipeline.log`.

**mAP 스윕 결과** (valid 150장, FP8 leave-one-in): 뚜렷한 민감 블록은
**final_heads(bbox −0.0144)와 decoder_layer_4(−0.0126)** 뿐이고 나머지
11개 블록은 ±0.004 노이즈 범위. threshold 0.001로는 13개 중 9개가
"민감" 과잉 판정 → mAP는 민감도 신호로 분해능이 부족 (§18 KLD 전환의
직접 근거).

**Q04/Q05 결과** (test 446장, RTX 5070 batch 1):

| 엔진 | 크기(MB) | Mask AP | AP_small | mIoU | FPS |
|---|---:|---:|---:|---:|---:|
| FP16 참조 | 73.5 | 0.6488 | 0.2160 | 0.8251 | 84.7 |
| **Q04 uniform FP8** | **41.0 (−44%)** | 0.6441 (−0.0047) | 0.2180 (유지) | 0.8225 | 83.9 |
| **Q05 mixed (mAP 기반)** | 65.7 (−11%) | **0.6489 (무손실)** | 0.2175 | 0.8253 | **86.2** |

**INT8(§16)과 정반대의 성공**: FP8은 uniform으로도 정확도 손실이
−0.005에 그치고(AP_small 붕괴 없음!) 크기를 44% 더 줄인다. Q05는
완전 무손실이지만 mAP 스윕의 과잉 민감 판정(9블록 FP16) 탓에 크기
이득이 11%에 그침 — 민감도 신호의 품질이 mixed-precision의 크기-정확도
효율을 직접 결정한다는 증거.

## 18. (5차 지시) KL divergence 기반 민감도 — Unsloth Dynamic 2.0 아이디어 이식

사용자 제안: mAP 같은 태스크 지표 외에 **KL divergence**로 양자화 품질을
재야 한다 (unsloth.ai Dynamic 2.0 GGUF의 접근). 채택 근거는 §17의 mAP
스윕 결과가 스스로 보여줬다 — FP8 블록별 mAP 하락이 ±0.004 노이즈에
묻혀 13개 중 9개가 "민감"으로 과잉 판정됐고, 뚜렷한 신호는
final_heads(+0.0144)와 decoder_layer_4(+0.0126)뿐이었다.

구현 (`scripts/lightweighting/kld_sensitivity.py`):

- FP32 원본 모델의 출력 분포를 캐시한 뒤, 블록 하나씩 FP8을 켜고
  **출력 분포 간 KLD**를 측정: ① 클래스 logits의 Bernoulli KLD
  (RF-DETR는 sigmoid/focal 방식이라 클래스별 Bernoulli가 원리적),
  ② mask logits(stride 4 서브샘플)의 Bernoulli KLD, ③ box L1 drift.
  랭킹은 kld_total = kld_cls + kld_mask.
- KLD는 COCO 평가보다 훨씬 싸므로 backbone을 **레이어 단위(12개)** 로
  쪼개 총 ~21블록의 세밀한 맵을 48장으로 측정 (mAP 스윕은 150장으로
  13블록이 한계였음).
- 상위 25% 블록을 민감(FP16 유지)으로 지정, 출력 JSON은
  `quantize_modelopt.py --sensitivity-report`와 스키마 호환.
- registry에 **Q06(fp8-mixed-kld-sensitivity)** 등록. 가설: 같은/더 작은
  FP16 예산으로 mAP-threshold 방식(Q05)보다 나은 정확도.
- 실행: `run_kld_pipeline.sh`가 FP8 파이프라인(Q04/Q05) 종료를 기다렸다가
  KLD 스윕 → Q06 quantize → 엔진 → 평가 → 벤치마크 자동 수행.

논문 관점: Q04(uniform FP8) vs Q05(mAP 기반 mixed) vs Q06(KLD 기반
mixed) 3자 비교는 "민감도 신호의 선택이 mixed-precision 품질을
좌우한다"는 ablation이 되고, LLM 양자화 방법론(imatrix→SmoothQuant,
KLD 민감도→Dynamic 2.0)의 비전 검출기 이식이라는 신규성 축을 이룬다.

### 18.1 방법론적 발견: per-index KLD는 DETR에서 실패한다 (v1 → v2)

1차 KLD 스윕(v1, 출력 인덱스별 비교)의 맵은 mAP 스윕과 정반대였다 —
backbone 전 블록이 kld_total ~4e-2로 균일하게 최상위, decoder_layer_4와
segmentation_head가 최하위. 그러나 backbone 블록들의 box drift가 0.13
(정규화 좌표의 13%)인데도 같은 블록의 mAP 하락은 ~0이라는 모순이 있다.

원인: **DETR의 query는 교환 가능(set prediction)** 하다. 초기 레이어의
미세한 섭동만으로도 "어느 query가 어느 객체를 잡는가"의 배정이 뒤바뀌고,
인덱스별 비교는 이 순열 변화를 분포 손상으로 오측정한다. LLM은 토큰
위치가 고정이라 Unsloth의 per-position KLD가 유효하지만, set-prediction
모델에 그대로 이식하면 실패한다 — **이식에는 순열 불변 비교가 필요**.

수정(v2): 참조 모델의 상위 50개 confident query를 후보 출력과
**Hungarian 매칭**(box L1 + class L1 비용)한 뒤 매칭된 쌍에서 KLD를
계산하도록 `kld_sensitivity.py`를 개정. v1 맵과 v1 기반 Q06 결과는
`*-perindex-v1*`로 보존하고 v2로 재실행 (`run_kld_v2.sh`).
이 발견 자체가 논문 §방법론에 쓸 가치가 있다.

### 18.2 KLD v2 결과와 두 번째 방법론적 발견

Hungarian 매칭으로 box drift가 0.13→0.008로 정상화됐고 sanity(전부
비활성)는 KLD 0을 확인. 그러나 **매칭 후에도 KLD 랭킹은 mAP 손상과
불일치**했다 — v2 맵의 상위는 여전히 backbone 계열(2.0~2.9e-3으로
균일)이고 mAP가 지목한 decoder_layer_4·final_heads는 하위권(0.4~2.6e-3),
segmentation_head는 0. v2 맵 기반 Q06 엔진(47.2MB)의 mask AP는
0.6438로 uniform FP8(Q04 0.6441)과 동일 — KLD가 고른 보호 블록은 남은
−0.005를 회복하지 못했고, mAP가 고른 블록(Q05)은 회복했다.

**해석: 검출 AP는 랭킹 지표다.** 출력단(마지막 decoder layer·head)의
양자화는 분포 관점에선 작은 변화지만 threshold·NMS 근방의 점수 순서를
직접 흔들어 AP를 깎는다. 반대로 backbone의 분포 drift는 크더라도
(매칭 후 KLD 기준) 점수 순서를 보존해 AP에 무해하다. 즉 **LLM의 KLD
민감도(다음 토큰 분포가 곧 최종 출력인 세계)는 랭킹 기반 태스크 지표를
쓰는 검출기에 직접 이식되지 않는다** — 이식하려면 (a) 순열 불변 매칭에
더해 (b) 분포 거리 대신 점수-순서 민감도(rank correlation, flips)를
재야 한다. Unsloth 아이디어의 검증적 실패 분석으로 논문 가치가 크다.

### 18.3 최종 구성: Q07 (targeted top-2) — 가설 검증 성공

위 결론의 실용 귀결로, mAP 스윕에서 노이즈 위로 올라온 **단 2개 블록
(final_heads, decoder_layer_4)만 FP16 유지**하고 나머지 전부 FP8인
Q07을 등록·실행했다.

**FP8 최종 비교표** (test 446장, RTX 5070 batch 1):

| 엔진 (FP16 유지 블록 수) | 크기(MB) | Mask AP | AP_small | mIoU | FPS |
|---|---:|---:|---:|---:|---:|
| FP16 참조 | 73.5 | 0.6488 | 0.2160 | 0.8251 | 84.7 |
| Q04 uniform FP8 (0) | **41.0** | 0.6441 | 0.2180 | 0.8225 | 83.9 |
| Q05 mAP-mixed (9) | 65.7 | **0.6489** | 0.2175 | 0.8253 | 86.2 |
| Q06 KLD-mixed (6) | 47.2 | 0.6438 | 0.1151 | 0.8204 | 85.2 |
| **Q07 targeted (2)** | **42.6** | **0.6468** | **0.2475** | **0.8253** | 83.3 |

- Q07은 uniform 대비 +1.6MB만으로 잔여 손실의 60%를 회복
  (0.6441→0.6468, FP16 대비 −0.002), mIoU는 최고치(0.8253) 동률,
  AP_small은 전체 최고(0.2475). **크기 −42% + 사실상 무손실** — 목표했던
  "최고 성능" 구성.
- 완전 무손실이 필요하면 Q05(−11% 크기), 최소 크기가 필요하면
  Q04(−44%, −0.005)가 대안. 민감도 신호별 결과(Q05 vs Q06 vs Q07)가
  그대로 ablation 표가 된다.
- 주의: FP8 커널은 sm89+(Ada/Blackwell) 전용. 평가 노트북(RTX 4050,
  Ada)에서는 재현 가능하나 RTX 3080(Ampere) 배포는 불가 — 논문에는
  "차세대 하드웨어 경로"로 위치시킬 것.

### §9 잔여분: general_mission FP16 속도 (GPU 해제 후 측정)

RTX 5070 batch 1: with_crosswalk **84.5 FPS**(11.84ms),
without_crosswalk **81.5 FPS**(12.27ms) — FP16 엔진으로 실시간 기준(30
FPS)을 큰 여유로 충족. (참고: 같은 장비에서 parking B01 FP16도 84.7 FPS,
논문 속도표의 RTX 4050 수치와는 장비가 달라 직접 비교 불가)

---
