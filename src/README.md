# Python 라이브러리

여러 실행 스크립트가 공유하는 구현을 `kips_lightweighting/` 패키지에 둔다.
CLI와 경로 조합은 `scripts/`, 모델·artifact·정적 분석 로직은 이 패키지가
담당한다.

구현 개요는 [kips_lightweighting/README.md](kips_lightweighting/README.md)를
참고한다.
