version: 2
rclone_remote: kips-drive
drive_root_folder_id: 1R5lKUurZMH6xGiUp-WRDZlCg-sc0bt7B

datasets:
  calibration:
    description: INT8 calibration images for front and rear models
    source_dir: calibration
    output_dir: data/calibration
    required_paths:
      - front/manifest.json
      - rear/manifest.json

  labeled-test:
    description: Labeled COCO test data for front and rear models
    source_dir: labeled_test
    output_dir: data/labeled_test
    required_paths:
      - front/_annotations.coco.json
      - rear/_annotations.coco.json
